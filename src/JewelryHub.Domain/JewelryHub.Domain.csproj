<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>JewelryHub.Domain</RootNamespace>
  </PropertyGroup>

  <!-- Intentionally no PackageReferences: the Domain layer must stay
       framework-free per Clean Architecture (no EF Core, no ASP.NET,
       no external SDKs). RowVersion uses byte[], which is a BCL type. -->

</Project>
