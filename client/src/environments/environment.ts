export const environment = {
  production: true,
  // Overridden per-deployment; this default assumes the API is reverse-proxied
  // under the same origin as the built SPA (e.g. /api/v1/...).
  apiUrl: '/api/v1',
};
